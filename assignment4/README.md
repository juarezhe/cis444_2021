# Fullstack Example

Expected format for `connection.yml` is as follows:
```
host: <value>
dbname: <value>
user: <value>
password: <value>
```

Expected format for `secret.txt` is as follows:
```
<value>
```